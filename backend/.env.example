# Copy this to backend/.env on the EC2 host (NOT inside any service folder).
# These vars are consumed by docker-compose.yml at `up` time.

# Comma-separated list of frontend origins allowed by Bastion CORS.
# In production this should be the CloudFront domain.
BASTION_ALLOWED_ORIGINS=https://app.yourdomain.com
